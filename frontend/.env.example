# FastAPI backend URL.
# Local dev: leave empty (Vite proxies /api to http://127.0.0.1:8000).
# Vercel production: set to Render backend URL, e.g. https://your-service.onrender.com
VITE_API_BASE_URL=

# Dev server port (default 5173). LAN sharing: 8080
VITE_DEV_PORT=
